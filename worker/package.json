{
  "name": "robot-sim-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "pnpm --filter web build",
    "dev": "pnpm build:web && wrangler dev",
    "deploy": "pnpm build:web && wrangler deploy",
    "types": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.0.0"
  }
}
