// Root build file. Individual modules live under their own subdirectories
// (see settings.gradle.kts). The pnpm workspace (web/, worker/) is
// deliberately independent of Gradle and is orchestrated by `justfile`.
plugins {}
